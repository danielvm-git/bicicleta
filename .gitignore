# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Playwright
playwright-report/
playwright/.cache/
test-results/
playwright-junit.xml

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# ── GSD self-heal: .gsd is a symlink to external state ──
.gsd

# ── GSD baseline (auto-generated) ──
.gsd-id
# Project MCP (Claude Code, root) — versioned; same intent as .cursor/ and .gemini/ (see docs/mcp-and-ides.md)
.bg-shell/
_bmad-output/
~$*.xlsx
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
node_modules/
.next/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
coverage/
.cache/
tmp/
.openhands
.claude
.agents
.agent/
.artifacts/
.gsd/
*.db
# BMAD install / module tree (local); version the root config only
_bmad/**
!_bmad/config.toml
# Ctxo local cache (never committed)
.ctxo/.cache/

# ── GSD baseline (auto-generated) ──
.mcp.json
